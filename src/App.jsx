import React from 'react';
import LandingPage from './pages/LandingPage';

export default function App() {
  // Directly passes rendering down to the UI assembly framework page
  return <LandingPage />;
}